package com.aliro2.controller;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PostMapping;

@Controller
public class ProveedorController {
    // @Autowired private ProveedorService proveedorService;

    // @GetMapping("/proveedores/crear") -> Muestra form-proveedor.html
    // @GetMapping("/proveedores/modificar") -> Muestra list-proveedores.html (con botón de editar)
    // @GetMapping("/proveedores/editar/{id}") -> Muestra form-proveedor.html (con datos)
    // @GetMapping("/proveedores/consultar") -> Muestra list-proveedores.html
    // @PostMapping("/proveedores/guardar") -> Guarda o actualiza Proveedor
}